/*
    [Déclaration tableau]
       <type> <nom tableau>[X] ;  Déclares avec valeurs aléatoires
       <type> <nom tableau>[X][X] : pour des tableaus avec ligne et colonnes

    [Initialisation]
        tableau[5] = {valeur1, valeur2, valeur3, valeur4, valeur5} ;
        tableau[5] = {0};  Toutes les cases valent 0
        tableau[5] = {4};  Première case à 4, le reste à 0


    [Accès au tableau]
        tableau[X]     : élément d'indice X(x+1ème élément du tableau)

        tableau        : adresse du tableau 
        *tableau       : valeur du premier element du tableau
        *(tableau + X) : element d'indice X

    (NOTE) 
        tableau[5] même chose que *(tableau + 5)
*/

#include <stdio.h>

/*--------------------------------------------------------------*/

int main(void){



    return 0 ; 
}

