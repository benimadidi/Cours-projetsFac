
<?php

/*
    =======================
    🧮 OPÉRATEURS EN PHP
    =======================

    ➕ ARITHMÉTIQUES
    - +, -, *, /, %, **             // Opérations classiques.
    - intdiv(a, b)                  // Division entière.
    - fmod(a, b)                    // Reste (modulo réel).

    📝 AFFECTATIONS
    - =, +=, -=, *=, /=, %=         // Assignations combinées.
    - ++, --                        // Incrémentation / Décrémentation.

    🔗 CONCATÉNATION
    - .                             // Concatène deux chaînes.
    - .=                            // Ajoute à une chaîne existante.

    🔁 LOGIQUES
    - and, &&                       // ET logique.
    - or,  ||                       // OU logique.
    - xor                           // OU exclusif (soit A, soit B, mais pas les deux).

*/

$a = true;
$b = true;


echo $a xor $b;

